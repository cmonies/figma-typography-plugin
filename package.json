{
  "name": "figma-typography-plugin",
  "version": "1.0.0",
  "description": "Figma plugin for generating typography systems with text styles, specimens, and design tokens",
  "main": "dist/code.js",
  "scripts": {
    "build": "node esbuild.config.js",
    "watch": "node esbuild.config.js --watch",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@figma/plugin-typings": "^1.100.0",
    "@types/node": "^20.10.0",
    "esbuild": "^0.20.0",
    "preact": "^10.19.0",
    "typescript": "^5.3.0"
  },
  "dependencies": {
    "js-yaml": "^4.1.0"
  }
}
